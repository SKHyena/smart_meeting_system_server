version: "3.3"

services:
  fastapi:
    image: smart_meeting_system_server
    command: uvicorn app.main:app --host 0.0.0.0 --port 80 --reload
    ports:
      - 80:80
    volumes:
      - ./app:/code/app
